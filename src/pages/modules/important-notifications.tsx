import { SharedModulePage } from './shared-module-page';

const config = { title: 'Important Notifications', description: 'Create placement drives, internships, and college announcements.' };

export default function ImportantNotificationsPage() {
  return <SharedModulePage config={config} />;
}
